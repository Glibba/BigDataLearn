#!/usr/bin/env python

import sys

words = {}

for line in sys.stdin:
    input = line.strip().split(' ')
    doc = input[0]

    prev_word = words.get()
    for word in input[1:]:
        if 
        print(word, value, 1)
